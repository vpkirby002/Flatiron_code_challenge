<%= form_for(superpower) do |f| %>
  <% if superpower.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(superpower.errors.count, "error") %> prohibited this superpower from being saved:</h2>

      <ul>
      <% superpower.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
